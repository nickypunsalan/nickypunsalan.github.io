import { Button, Row } from 'react-bootstrap';

function FilterButton() {
    return (
        <Row>
        <Button className="d-inline">Test</Button>
        <Button className="d-inline">Test</Button>
        <Button className="d-inline">Test</Button>
    </Row>
    );
}
export default FilterButton;